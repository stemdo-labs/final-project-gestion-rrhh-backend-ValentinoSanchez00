pipeline {
    agent any
    triggers {
        pollSCM('')
    }
    stages {
        stage('Build') {
            when {
                changeset "**/pom.xml"
            }
            steps {
                echo "Solo construyo si hay cambios en mi carpeta"
                
            }
        }
    }
}
